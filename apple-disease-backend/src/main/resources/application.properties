# ===============================
# SERVER
# ===============================
server.port=8081

# ===============================
# DATABASE CONFIG (PostgreSQL)
# ===============================
spring.datasource.url=${DB_URL:jdbc:postgresql://apple-postgres:5432/leafsnap}
spring.datasource.username=${DB_USERNAME:leafsnap_user}
spring.datasource.password=${DB_PASSWORD:T0fEiuzSqaIpEeMd7QS2MYjfsZD4UZMA}
spring.datasource.driver-class-name=org.postgresql.Driver

# ===============================
# JPA / HIBERNATE
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.jdbc.time_zone=Asia/Kolkata
spring.jackson.time-zone=Asia/Kolkata

# ===============================
# ML SERVICE URL
# ===============================
ml.api.url=${ML_API_URL:http://apple-ml:5000/predict}
